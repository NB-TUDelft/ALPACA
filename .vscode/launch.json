{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (with Sync)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "preLaunchTask": "Alpaca Sync"
        }
    ]
}